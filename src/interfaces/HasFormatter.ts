export interface HasFormatter {
	add(): string;

}


/* interface Person {
	name: string;
	position: string;
	giveTime(days: number): number;
}

const paul: Person = {
	name: 'paul',
	position: 'manager',
	giveTime(days: number): number {
		return days;
	}
}; */